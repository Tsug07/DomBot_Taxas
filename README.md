# DomBot_Taxas
Bot dedicado a emitir taxas sindicais

Este script cria uma interface simples para:
- informar a rubrica (padrão 243)
- escolher a pasta de destino do arquivo
- abrir o DomBot Admiss e preparar o envio das teclas `F` e `M`

Execute em Python com:

```bash
python taxa_bares.py
```
